name: Deploy
on:
  workflow_dispatch:
  push:
    branches:
      - master
jobs:
  build:
    name: Run for ${{ env.GITHUB_REF }}
    runs-on: ubuntu-latest
    steps:
      - name: Build API
        run: |
          echo ${GITHUB_REF}
          echo "Build API"
          sleep 10s