name: Deploy
on:
  workflow_dispatch:
  push:
    branches:
      - master
jobs:
  caption: ${{ GITHUB_REF }}
  build:
    name: Run API
    runs-on: ubuntu-latest
    steps:
      - name: Build API
        run: |
          echo ${GITHUB_REF}
          echo "Build API"
          sleep 10s